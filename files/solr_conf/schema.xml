<?xml version="1.0" encoding="UTF-8" ?>

<schema name="orderHandling" version="1.5">
  <types>
    <!-- The StrField type is not analyzed, but indexed/stored verbatim. -->
    <fieldType name="string" class="solr.StrField" sortMissingLast="true" />
    <!-- The format for this date field is of the form 1995-12-31T23:59:59Z, and
         is a more restricted form of the canonical representation of dateTime
         http://www.w3.org/TR/xmlschema-2/#dateTime    
         The trailing "Z" designates UTC time and is mandatory.
         Optional fractional seconds are allowed: 1995-12-31T23:59:59.999Z
         All other components are mandatory.

         Expressions can also be used to denote calculations that should be
         performed relative to "NOW" to determine the value, ie...

               NOW/HOUR
                  ... Round to the start of the current hour
               NOW-1DAY
                  ... Exactly 1 day prior to now
               NOW/DAY+6MONTHS+3DAYS
                  ... 6 months and 3 days in the future from the start of
                      the current day
                      
         Consult the DateField javadocs for more information.

         Note: For faster range queries, consider the tdate type
      -->
    <fieldType name="date" class="solr.TrieDateField" precisionStep="0" positionIncrementGap="0"/>

    <!-- A Trie based date field for faster date range queries and date faceting. -->
    <fieldType name="tdate" class="solr.TrieDateField" precisionStep="6" positionIncrementGap="0"/>

 </types>


 <fields>
   <field name="id" type="string" indexed="true" stored="true" required="true" /> 
   <field name="invoice_number" type="string" indexed="true" stored="true" required="false" /> 
   <field name="reservation_number" type="string" indexed="true" stored="true" required="false" /> 
   <field name="currency" type="string" indexed="false" stored="true" required="true" /> 
   <field name="create_date" type="date" indexed="false" stored="true" required="true" /> 
   <field name="order_ref1" type="string" indexed="true" stored="true" required="false" /> 
   <field name="order_ref2" type="string" indexed="true" stored="true" required="false" /> 
   <field name="estore_id" type="string" indexed="true" stored="true" required="true" /> 
 </fields>

 <!-- Field to use to determine and enforce document uniqueness. 
      Unless this field is marked with required="false", it will be a required field
   -->
 <uniqueKey>id</uniqueKey>

</schema>
